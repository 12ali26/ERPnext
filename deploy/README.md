# Deploy Assets

This folder is reserved for deployment-specific files.

For the first pilot, prefer a normal hosted Frappe/ERPNext server and install the custom app from `apps/business_customizations`.

Do not deploy from Codespaces. Codespaces is useful for development and demos, but it is not a stable production runtime.

